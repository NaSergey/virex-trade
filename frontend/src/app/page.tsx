import { redirect } from 'next/navigation';

/**
 * Корень адреса — не страница, а указатель.
 *
 * Раньше весь продукт жил здесь одним роутом, а раздел выбирался состоянием
 * внутри: адрес не менялся, и перезагрузка любой страницы возвращала человека
 * на Обзор — вместе с потерянными фильтрами, периодом и местом в таблице.
 * Теперь у каждого раздела свой путь, а `/` только отправляет на первый из них.
 *
 * Редирект серверный: на клиенте он был бы лишним кадром пустой страницы.
 */
export default function Home() {
  redirect('/overview');
}
