/**
 * Учётка витрины: её вводит кнопка «Посмотреть демо» на странице входа.
 *
 * Пароль лежит в коде фронтенда открыто, и это не утечка, а условие работы
 * кнопки: демо на то и демо, чтобы в него пускали без регистрации. Отдельного
 * эндпоинта «войти как демо» здесь нет намеренно — он был бы вторым путём
 * авторизации, который придётся охранять наравне с настоящим, ради того же
 * самого результата.
 *
 * Аккаунт общий: всё, что посетитель разметит или удалит, увидят следующие.
 * Раскладывается заново прогоном `backend/src/scripts/seed-demo.ts` — там же
 * дублируются эти две строки, потому что бэкенд и фронтенд не делят модули.
 */
export const DEMO_EMAIL = 'demo@example.com';
export const DEMO_PASSWORD = 'demo1234';
