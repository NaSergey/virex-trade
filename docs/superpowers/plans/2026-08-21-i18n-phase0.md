# Локализация Virex на английский — Phase 0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development (recommended) or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать пользователю переключатель RU/EN в шапке, который переводит первый экран,
который видит новый пользователь: страницу входа/регистрации, шапку (`TopNav`) и экран
восстановления сессии — плюс инфраструктуру (каталоги сообщений, форматирование, обработку
ошибок API), на которую лягут все последующие фазы (Обзор, Теги, Выборка, Рынок, Настройки)
без повторного архитектурного решения.

**Architecture:** `next-intl` без routing/middleware — локаль живёт в React-контексте
(`LocaleProvider`), персистится в `localStorage`, каталоги сообщений — статические JSON по
FSD-неймспейсам (`common`, `nav`, `auth`, `errors`). Бэкенд-исключения получают аддитивное поле
`code` вместо i18n-библиотеки; фронт переводит по коду через модуль-level реестр, который
`LocaleProvider` обновляет при смене языка (тот же паттерн, что уже есть в `http.ts` для
`setUnauthenticatedHandler`).

**Tech Stack:** Next.js 16 (App Router, но всё приложение — client-rendered SPA-оболочка без
реального роутинга страниц), React 19, `next-intl` (новая зависимость), NestJS 10, vitest
(frontend, `environment: 'node'`, только тесты чистых функций — компонентных тестов в проекте
нет и в этом плане не заводится), Jest (backend, `*.spec.ts`).

## Global Constraints

- Спек: `docs/superpowers/specs/2026-08-21-i18n-english-design.md`.
- Без URL-роутинга по локали (`/en/...`) — приложение не имеет реальных Next-роутов кроме `/`
  и `/login`, вкладки переключаются React-стейтом.
- Без auto-detect языка браузера. Дефолт — всегда `'ru'`, меняется только явным действием
  пользователя, персистится в `localStorage`.
- Без i18n-библиотеки на бэкенде (без `nestjs-i18n`). Только аддитивное поле `code` в теле
  исключения; текст на бэкенде остаётся как fallback.
- В этой фазе коды получает **только `auth.service.ts`**. Остальные ~34 сообщения об ошибках
  на бэкенде — задача будущих фаз (roadmap в спеке, раздел «Фазы»).
- Валидационные сообщения `class-validator` (например, `register.dto.ts:8` — «Пароль должен
  быть не короче 8 символов») используют другой механизм (`ValidationPipe`, `message: string[]`
  без места для `code`) и **не переводятся в этой фазе**. Клиентский `minLength={8}` на поле
  пароля уже перехватывает большинство случаев; сообщение с сервера — редкий край (JS
  отключён/обойдён), `resolveApiError` в этом случае просто вернёт русский текст как есть —
  это принятое и осознанное ограничение Phase 0, не баг.
- Telegram-бот и его сообщения — вне периметра, не трогаем.
- Не заводить новую тестовую инфраструктуру (React Testing Library, jsdom). У тестируемого в
  этой фазе UI (провайдер, компоненты) нет пригодного для юнит-теста поведения без DOM —
  верификация таких шагов через `tsc --noEmit` + `eslint` + (где отмечено) ручной прогон
  `next build`/`next dev`, не через vitest.
- Комментарии в коде остаются русскими (конвенция проекта, `CLAUDE.md`) — переводится только
  пользовательский текст (JSX-контент, атрибуты `aria-label`, сообщения об ошибках).
- FSD: инфраструктура локализации живёт в `shared/` (`shared/i18n`, `shared/ui`, `shared/api`,
  `shared/lib`), не в `views/` или `widgets/`.
- Пакетный менеджер обоих проектов — npm (`package-lock.json`), не pnpm/yarn.

---

## Task 1: Хранилище выбранного языка

**Files:**
- Create: `frontend/src/shared/i18n/locale-storage.ts`
- Test: `frontend/src/shared/i18n/locale-storage.test.ts`

**Interfaces:**
- Produces: `type Locale = 'ru' | 'en'`; `SUPPORTED_LOCALES: readonly Locale[]`;
  `getStoredLocale(storage?: Pick<Storage, 'getItem'>): Locale`;
  `setStoredLocale(locale: Locale, storage?: Pick<Storage, 'setItem'>): void`.
  Оба принимают необязательный `storage` — по умолчанию читают `window.localStorage`, если
  `window` определён (SSR-safe), иначе `undefined`. Параметр существует в первую очередь ради
  тестируемости без jsdom.

- [ ] **Step 1: Написать падающий тест**

```ts
// frontend/src/shared/i18n/locale-storage.test.ts
import { describe, expect, it } from 'vitest';
import { getStoredLocale, setStoredLocale } from './locale-storage';

function fakeStorage(initial: Record<string, string> = {}) {
  const data = { ...initial };
  return {
    getItem: (key: string) => data[key] ?? null,
    setItem: (key: string, value: string) => {
      data[key] = value;
    },
  };
}

describe('getStoredLocale', () => {
  it('возвращает ru по умолчанию, если ничего не сохранено', () => {
    expect(getStoredLocale(fakeStorage())).toBe('ru');
  });

  it('возвращает сохранённый язык', () => {
    expect(getStoredLocale(fakeStorage({ 'virex-locale': 'en' }))).toBe('en');
  });

  it('игнорирует неизвестное значение в хранилище и возвращает дефолт', () => {
    expect(getStoredLocale(fakeStorage({ 'virex-locale': 'fr' }))).toBe('ru');
  });

  it('без storage (SSR) — дефолт ru, без исключения', () => {
    expect(getStoredLocale(undefined)).toBe('ru');
  });
});

describe('setStoredLocale', () => {
  it('сохраняет язык под тем же ключом, что читает getStoredLocale', () => {
    const storage = fakeStorage();
    setStoredLocale('en', storage);
    expect(getStoredLocale(storage)).toBe('en');
  });
});
```

- [ ] **Step 2: Запустить тест, убедиться, что падает**

Run: `cd frontend && npx vitest run src/shared/i18n/locale-storage.test.ts`
Expected: FAIL — `Cannot find module './locale-storage'` (файла ещё нет).

- [ ] **Step 3: Реализовать `locale-storage.ts`**

```ts
// frontend/src/shared/i18n/locale-storage.ts
export type Locale = 'ru' | 'en';

export const SUPPORTED_LOCALES: readonly Locale[] = ['ru', 'en'];

const STORAGE_KEY = 'virex-locale';
const DEFAULT_LOCALE: Locale = 'ru';

function isLocale(value: string | null): value is Locale {
  return value === 'ru' || value === 'en';
}

function browserStorage(): Storage | undefined {
  return typeof window === 'undefined' ? undefined : window.localStorage;
}

/**
 * Читает сохранённый язык интерфейса. На сервере (первый SSR-проход) storage
 * недоступен — там всегда дефолт `'ru'`, а не исключение.
 */
export function getStoredLocale(storage: Pick<Storage, 'getItem'> | undefined = browserStorage()): Locale {
  const raw = storage?.getItem(STORAGE_KEY) ?? null;
  return isLocale(raw) ? raw : DEFAULT_LOCALE;
}

export function setStoredLocale(
  locale: Locale,
  storage: Pick<Storage, 'setItem'> | undefined = browserStorage(),
): void {
  storage?.setItem(STORAGE_KEY, locale);
}
```

- [ ] **Step 4: Запустить тест, убедиться, что проходит**

Run: `cd frontend && npx vitest run src/shared/i18n/locale-storage.test.ts`
Expected: PASS, 5 tests.

- [ ] **Step 5: Коммит**

```bash
git add frontend/src/shared/i18n/locale-storage.ts frontend/src/shared/i18n/locale-storage.test.ts
git commit -m "i18n: добавить хранилище выбранного языка"
```

---

## Task 2: Каталоги сообщений ru/en

**Files:**
- Create: `frontend/src/shared/i18n/messages/ru.json`
- Create: `frontend/src/shared/i18n/messages/en.json`
- Test: `frontend/src/shared/i18n/messages.test.ts`

**Interfaces:**
- Produces: JSON-модули с неймспейсами `common`, `nav`, `auth`, `errors`. Точная форма (все
  ключи ниже) — контракт, на который опираются Task 4 (`errors`), Task 6 (`nav`, `common.language`),
  Task 7 (`auth`), Task 8 (`common.restoringSession`).

- [ ] **Step 1: Написать падающий тест на паритет ключей**

```ts
// frontend/src/shared/i18n/messages.test.ts
import { describe, expect, it } from 'vitest';
import ru from './messages/ru.json';
import en from './messages/en.json';

function deepKeys(obj: Record<string, unknown>, prefix = ''): string[] {
  return Object.entries(obj).flatMap(([key, value]) => {
    const path = prefix ? `${prefix}.${key}` : key;
    if (value && typeof value === 'object' && !Array.isArray(value)) {
      return deepKeys(value as Record<string, unknown>, path);
    }
    return [path];
  });
}

describe('каталоги сообщений', () => {
  it('ru и en содержат один и тот же набор ключей — иначе useTranslations молча упадёт на дефолт next-intl для отсутствующего ключа', () => {
    expect(deepKeys(en).sort()).toEqual(deepKeys(ru).sort());
  });
});
```

- [ ] **Step 2: Запустить тест, убедиться, что падает**

Run: `cd frontend && npx vitest run src/shared/i18n/messages.test.ts`
Expected: FAIL — `Cannot find module './messages/ru.json'`.

- [ ] **Step 3: Создать `messages/ru.json`**

```json
{
  "common": {
    "restoringSession": "Восстанавливаем сессию…",
    "language": "Язык"
  },
  "nav": {
    "sections": "Разделы",
    "overview": "Обзор",
    "tags": "Теги",
    "lab": "Выборка",
    "analytics": "Рынок",
    "settings": "Настройки",
    "profile": "Профиль",
    "logout": "Выйти",
    "who": "кто",
    "email": "почта",
    "noName": "без имени"
  },
  "auth": {
    "brand": "Virex",
    "loginTitle": "Вход",
    "registerTitle": "Регистрация",
    "loginLede": "Журнал криптофьючерсов: сделки, разметка и статистика по ней.",
    "registerLede": "Создайте аккаунт — дальше подключите биржевые ключи в настройках.",
    "nameLabel": "Имя (необязательно)",
    "emailLabel": "Почта",
    "passwordLabel": "Пароль",
    "submitLogin": "Войти",
    "submitRegister": "Зарегистрироваться",
    "submitting": "Подождите…",
    "switchToRegister": "Нет аккаунта? Зарегистрироваться",
    "switchToLogin": "Уже есть аккаунт? Войти",
    "genericError": "Ошибка"
  },
  "errors": {
    "INVALID_CREDENTIALS": "Неверный email или пароль",
    "USER_EXISTS": "Пользователь с таким email уже существует",
    "REFRESH_TOKEN_MISSING": "Refresh-токен отсутствует",
    "REFRESH_TOKEN_INVALID": "Недействительный refresh-токен",
    "REFRESH_TOKEN_EXPIRED": "Refresh-токен истёк",
    "USER_NOT_FOUND": "Пользователь не найден",
    "generic": "Ошибка авторизации"
  }
}
```

- [ ] **Step 4: Создать `messages/en.json`**

```json
{
  "common": {
    "restoringSession": "Restoring session…",
    "language": "Language"
  },
  "nav": {
    "sections": "Sections",
    "overview": "Overview",
    "tags": "Tags",
    "lab": "Lab",
    "analytics": "Market",
    "settings": "Settings",
    "profile": "Profile",
    "logout": "Log out",
    "who": "who",
    "email": "email",
    "noName": "unnamed"
  },
  "auth": {
    "brand": "Virex",
    "loginTitle": "Log in",
    "registerTitle": "Sign up",
    "loginLede": "Crypto futures journal: trades, tagging, and the stats built on it.",
    "registerLede": "Create an account — connect exchange keys in settings next.",
    "nameLabel": "Name (optional)",
    "emailLabel": "Email",
    "passwordLabel": "Password",
    "submitLogin": "Log in",
    "submitRegister": "Sign up",
    "submitting": "Please wait…",
    "switchToRegister": "No account? Sign up",
    "switchToLogin": "Already have an account? Log in",
    "genericError": "Error"
  },
  "errors": {
    "INVALID_CREDENTIALS": "Invalid email or password",
    "USER_EXISTS": "A user with this email already exists",
    "REFRESH_TOKEN_MISSING": "Refresh token missing",
    "REFRESH_TOKEN_INVALID": "Invalid refresh token",
    "REFRESH_TOKEN_EXPIRED": "Refresh token expired",
    "USER_NOT_FOUND": "User not found",
    "generic": "Authorization error"
  }
}
```

- [ ] **Step 5: Запустить тест, убедиться, что проходит**

Run: `cd frontend && npx vitest run src/shared/i18n/messages.test.ts`
Expected: PASS.

- [ ] **Step 6: Коммит**

```bash
git add frontend/src/shared/i18n/messages frontend/src/shared/i18n/messages.test.ts
git commit -m "i18n: добавить каталоги сообщений ru/en"
```

---

## Task 3: `resolveApiError` — перевод ошибок API по коду

**Files:**
- Modify: `frontend/src/shared/api/http.ts`
- Test: `frontend/src/shared/api/http.test.ts` (создать — файла раньше не было)

**Interfaces:**
- Consumes: ничего нового (модуль уже существует).
- Produces: `setErrorMessages(messages: Record<string, string>): void` — вызывается из
  `LocaleProvider` (Task 4) при каждой смене локали, значение — плоский объект `errors` из
  `messages/{locale}.json`. `resolveApiError(body: { code?: string; message?: string | string[]; error?: string }, fallback?: string): string`.
  `apiJson` (уже существует в файле) начинает использовать `resolveApiError` вместо ручной
  сборки сообщения.

- [ ] **Step 1: Написать падающие тесты**

```ts
// frontend/src/shared/api/http.test.ts
import { describe, expect, it } from 'vitest';
import { resolveApiError, setErrorMessages } from './http';

describe('resolveApiError', () => {
  it('переводит по code, если для него есть текст в текущем каталоге', () => {
    setErrorMessages({ INVALID_CREDENTIALS: 'Invalid email or password', generic: 'Error' });
    expect(
      resolveApiError({ code: 'INVALID_CREDENTIALS', message: 'Неверный email или пароль' }),
    ).toBe('Invalid email or password');
  });

  it('падает на сырой message, если код не знаком текущему каталогу', () => {
    setErrorMessages({ generic: 'Error' });
    expect(resolveApiError({ code: 'SOME_NEW_CODE', message: 'Что-то пошло не так' })).toBe(
      'Что-то пошло не так',
    );
  });

  it('склеивает message-массив (class-validator) через запятую', () => {
    setErrorMessages({ generic: 'Error' });
    expect(
      resolveApiError({ message: ['Пароль слишком короткий', 'Email обязателен'] }),
    ).toBe('Пароль слишком короткий, Email обязателен');
  });

  it('без code/message/error и без явного fallback — берёт errors.generic из каталога', () => {
    setErrorMessages({ generic: 'Authorization error' });
    expect(resolveApiError({})).toBe('Authorization error');
  });

  it('явный fallback перебивает errors.generic', () => {
    setErrorMessages({ generic: 'Authorization error' });
    expect(resolveApiError({}, 'HTTP error! status: 500')).toBe('HTTP error! status: 500');
  });
});
```

- [ ] **Step 2: Запустить тесты, убедиться, что падают**

Run: `cd frontend && npx vitest run src/shared/api/http.test.ts`
Expected: FAIL — `resolveApiError`/`setErrorMessages` не существуют.

- [ ] **Step 3: Добавить `resolveApiError`/`setErrorMessages`, переключить `apiJson`**

В `frontend/src/shared/api/http.ts` добавить перед `apiJson` (после существующего блока
`setUnauthenticatedHandler` — тот же паттерн: модуль-level состояние + сеттер):

```ts
// Текущий перевод ошибок API. LocaleProvider вызывает setErrorMessages при
// каждой смене языка — тот же приём, что setUnauthenticatedHandler выше:
// модуль вне React, но должен реагировать на локаль без прокидывания её
// через каждый вызов apiFetch/apiJson.
let errorMessages: Record<string, string> = {};

export function setErrorMessages(messages: Record<string, string>): void {
  errorMessages = messages;
}

interface ApiErrorBody {
  code?: string;
  message?: string | string[];
  error?: string;
}

/**
 * Переводит ошибку API в текст текущего языка: по `code`, если для него
 * есть перевод в каталоге текущей локали. Иначе — сырой `message`/`error` с
 * бэкенда как раньше (обычно по-русски, пока код не заведён на этом
 * эндпоинте — так ведут себя все ещё не мигрированные на code эндпоинты).
 * Если нет вообще ничего — `fallback`, а если и его не передали, то
 * `errors.generic` из текущего каталога.
 */
export function resolveApiError(body: ApiErrorBody, fallback?: string): string {
  if (body.code && errorMessages[body.code]) return errorMessages[body.code];
  const raw = Array.isArray(body.message) ? body.message.join(', ') : body.message;
  if (raw) return raw;
  if (body.error) return body.error;
  return fallback ?? errorMessages.generic ?? 'Error';
}
```

Заменить тело `apiJson`:

```ts
export async function apiJson<T>(path: string, options?: RequestInit): Promise<T> {
  const res = await apiFetch(path, options);
  const data = await res.json().catch(() => ({}));
  const body = data as { success?: boolean } & ApiErrorBody;
  if (!res.ok || body.success === false) {
    throw new Error(resolveApiError(body, `HTTP error! status: ${res.status}`));
  }
  return data as T;
}
```

- [ ] **Step 4: Запустить тесты, убедиться, что проходят**

Run: `cd frontend && npx vitest run src/shared/api/http.test.ts`
Expected: PASS, 5 tests.

- [ ] **Step 5: Typecheck и lint файла**

Run: `cd frontend && npx tsc --noEmit -p tsconfig.json && npx eslint src/shared/api/http.ts --max-warnings=0`
Expected: без ошибок (существующие вызовы `apiJson` не меняли сигнатуру — обратной
совместимости ничего не угрожает).

- [ ] **Step 6: Коммит**

```bash
git add frontend/src/shared/api/http.ts frontend/src/shared/api/http.test.ts
git commit -m "i18n: перевод ошибок API по коду через resolveApiError"
```

---

## Task 4: `next-intl` + `LocaleProvider`

**Files:**
- Modify: `frontend/package.json` (добавить зависимость)
- Create: `frontend/src/shared/i18n/LocaleProvider.tsx`
- Create: `frontend/src/shared/i18n/index.ts`
- Modify: `frontend/src/app/layout.tsx`

**Interfaces:**
- Consumes: `getStoredLocale`/`setStoredLocale`/`Locale` (Task 1), `messages/{ru,en}.json`
  (Task 2), `setErrorMessages` (Task 3).
- Produces: `<LocaleProvider>` (оборачивает дерево, отдаёт `NextIntlClientProvider` детям —
  всё, что ниже, может звать `useTranslations(namespace)` из пакета `next-intl`);
  `useLocaleControl(): { locale: Locale; setLocale: (locale: Locale) => void }` — экспортируется
  из `@/shared/i18n`, нужен только компонентам, которые сами меняют язык (в этой фазе — только
  `LocaleSwitch`, Task 5). Названо не `useLocale`, чтобы не конфликтовать с одноимённым
  read-only хуком, который экспортирует сам пакет `next-intl`.

- [ ] **Step 1: Установить `next-intl`**

Run: `cd frontend && npm install next-intl`
Expected: пакет добавлен в `dependencies` в `package.json` и `package-lock.json`. Если npm
выдаёт предупреждение о peer dependency на React/Next — это некритично (warning, не error);
если это error (конфликт версий не резолвится), запустить `npm install next-intl --legacy-peer-deps`
и явно упомянуть это в коммите.

- [ ] **Step 2: Написать `LocaleProvider.tsx`**

```tsx
// frontend/src/shared/i18n/LocaleProvider.tsx
'use client';

import { createContext, useContext, useEffect, useMemo, useState, type ReactNode } from 'react';
import { NextIntlClientProvider } from 'next-intl';
import { setErrorMessages } from '@/shared/api/http';
import { getStoredLocale, setStoredLocale, type Locale } from './locale-storage';
import ru from './messages/ru.json';
import en from './messages/en.json';

const MESSAGES = { ru, en } as const satisfies Record<Locale, unknown>;

interface LocaleContextValue {
  locale: Locale;
  setLocale: (locale: Locale) => void;
}

const LocaleContext = createContext<LocaleContextValue | undefined>(undefined);

/**
 * Локаль — состояние приложения, не адреса: реальных Next-роутов у продукта
 * почти нет (вкладки переключает React-стейт, см. AppShell), поэтому здесь
 * нет ни middleware next-intl, ни сегмента [locale] — только контекст поверх
 * NextIntlClientProvider.
 */
export function LocaleProvider({ children }: { children: ReactNode }) {
  const [locale, setLocaleState] = useState<Locale>(() => getStoredLocale());

  useEffect(() => {
    document.documentElement.lang = locale;
    setStoredLocale(locale);
    setErrorMessages(MESSAGES[locale].errors);
  }, [locale]);

  const value = useMemo<LocaleContextValue>(() => ({ locale, setLocale: setLocaleState }), [locale]);

  return (
    <LocaleContext.Provider value={value}>
      <NextIntlClientProvider locale={locale} messages={MESSAGES[locale]}>
        {children}
      </NextIntlClientProvider>
    </LocaleContext.Provider>
  );
}

/** Только для компонентов, которые сами меняют язык (сейчас — LocaleSwitch). */
export function useLocaleControl(): LocaleContextValue {
  const ctx = useContext(LocaleContext);
  if (!ctx) throw new Error('useLocaleControl must be used within LocaleProvider');
  return ctx;
}
```

- [ ] **Step 3: Написать барель `index.ts`**

```ts
// frontend/src/shared/i18n/index.ts
export { LocaleProvider, useLocaleControl } from './LocaleProvider';
export type { Locale } from './locale-storage';
```

- [ ] **Step 4: Подключить в `layout.tsx`**

В `frontend/src/app/layout.tsx` добавить импорт `import { LocaleProvider } from '@/shared/i18n';`
и обернуть дерево:

```tsx
<body>
  <QueryProvider>
    <LocaleProvider>
      <AuthProvider>
        {children}
      </AuthProvider>
    </LocaleProvider>
  </QueryProvider>
</body>
```

- [ ] **Step 5: Typecheck, lint, сборка**

Run: `cd frontend && npx tsc --noEmit -p tsconfig.json && npx eslint src/shared/i18n src/app/layout.tsx --max-warnings=0 && npx next build`
Expected: без ошибок. Это первое место, где в дерево реально попадает `next-intl` —
`next build` здесь оправдан (новая зависимость, правило CLAUDE.md «крупное изменение → билд
запускать»), а не только typecheck.

- [ ] **Step 6: Коммит**

```bash
git add frontend/package.json frontend/package-lock.json frontend/src/shared/i18n/LocaleProvider.tsx frontend/src/shared/i18n/index.ts frontend/src/app/layout.tsx
git commit -m "i18n: подключить next-intl и LocaleProvider"
```

---

## Task 5: Locale-параметр в `formatTradeDate`

**Files:**
- Modify: `frontend/src/shared/lib/utils/format.ts:102-109`
- Modify: `frontend/src/shared/lib/utils/format.test.ts`

**Interfaces:**
- Produces: `formatTradeDate(iso: string, locale?: string): string` — `locale` необязателен,
  дефолт `'ru-RU'`, так что все существующие вызовы (`TradesTable`, `TradeOrders`,
  `TradeTagsModal` и др. — вне периметра Phase 0) продолжают работать без изменений.
  Аргумент — строка вида `'ru-RU'`/`'en-US'` (то, что ждёт `Intl`/`toLocaleString`), не наш
  `Locale` (`'ru'`/`'en'`) — маппинг `Locale → BCP-47` понадобится странице, которая реально
  покажет дату на английском (будущая фаза), здесь только сама возможность передать локаль.

- [ ] **Step 1: Дописать падающий тест**

В `frontend/src/shared/lib/utils/format.test.ts`, внутри `describe('formatTradeDate', ...)`,
добавить:

```ts
  it('принимает locale и форматирует по нему', () => {
    const iso = '2026-07-09T14:23:00Z';
    const expected = new Date(iso).toLocaleString('en-US', {
      day: '2-digit',
      month: '2-digit',
      hour: '2-digit',
      minute: '2-digit',
    });
    expect(formatTradeDate(iso, 'en-US')).toBe(expected);
  });
```

- [ ] **Step 2: Запустить тест, убедиться, что падает**

Run: `cd frontend && npx vitest run src/shared/lib/utils/format.test.ts`
Expected: FAIL — вызов с двумя аргументами не даёт того же результата, что с `'ru-RU'`
(текущая реализация игнорирует второй параметр, потому что его нет).

- [ ] **Step 3: Добавить параметр**

В `frontend/src/shared/lib/utils/format.ts` заменить:

```ts
/**
 * Format an ISO date string as a short day/month, hour:minute string.
 * @param iso - ISO date string
 * @param locale - BCP-47 локаль для Intl (по умолчанию 'ru-RU')
 * @returns Formatted date string
 */
export function formatTradeDate(iso: string, locale = 'ru-RU'): string {
  return new Date(iso).toLocaleString(locale, {
    day: '2-digit',
    month: '2-digit',
    hour: '2-digit',
    minute: '2-digit',
  });
}
```

- [ ] **Step 4: Запустить тесты, убедиться, что проходят**

Run: `cd frontend && npx vitest run src/shared/lib/utils/format.test.ts`
Expected: PASS, все тесты файла (старый тест на `'ru-RU'` по умолчанию тоже должен остаться
зелёным — сигнатура обратно совместима).

- [ ] **Step 5: Коммит**

```bash
git add frontend/src/shared/lib/utils/format.ts frontend/src/shared/lib/utils/format.test.ts
git commit -m "i18n: locale-параметр в formatTradeDate"
```

---

## Task 6: `LocaleSwitch` и локализация `TopNav`

**Files:**
- Create: `frontend/src/shared/ui/LocaleSwitch.tsx`
- Modify: `frontend/src/widgets/top-nav/TopNav.tsx`

**Interfaces:**
- Consumes: `useLocaleControl` (Task 4), `Seg`/`SegOption` (существующий `shared/ui/Seg.tsx`),
  неймспейсы `common`/`nav` из каталогов сообщений (Task 2) через `useTranslations` из
  `next-intl`.
- Produces: `<LocaleSwitch />` — самодостаточный компонент без пропов, использовать в любой
  части UI, где нужен переключатель языка (в этой фазе — только `TopNav`).

- [ ] **Step 1: Написать `LocaleSwitch.tsx`**

```tsx
// frontend/src/shared/ui/LocaleSwitch.tsx
'use client';

import { useTranslations } from 'next-intl';
import { Seg } from './Seg';
import { useLocaleControl, type Locale } from '@/shared/i18n';

const OPTIONS: { value: Locale; label: string }[] = [
  { value: 'ru', label: 'RU' },
  { value: 'en', label: 'EN' },
];

/** Переключатель языка интерфейса — тот же визуальный паттерн Seg, что у переключателя инструмента на странице «Рынок». */
export function LocaleSwitch() {
  const { locale, setLocale } = useLocaleControl();
  const t = useTranslations('common');
  return <Seg options={OPTIONS} value={locale} onChange={setLocale} ariaLabel={t('language')} />;
}
```

- [ ] **Step 2: Локализовать `TopNav.tsx`**

Заменить статичный массив `NAV` (сейчас хранит готовые русские `label`) на массив с ключами
перевода, и завести `t` внутри компонента:

```tsx
const NAV: { id: Tab; labelKey: 'overview' | 'tags' | 'lab' | 'analytics' | 'settings' }[] = [
  { id: 'overview', labelKey: 'overview' },
  { id: 'tags', labelKey: 'tags' },
  { id: 'lab', labelKey: 'lab' },
  { id: 'analytics', labelKey: 'analytics' },
  { id: 'settings', labelKey: 'settings' },
];
```

Добавить импорты:

```ts
import { useTranslations } from 'next-intl';
import { LocaleSwitch } from '@/shared/ui/LocaleSwitch';
```

Внутри `TopNav`, сразу после существующих `const { user, logout } = useAuth();`, добавить:

```ts
  const t = useTranslations('nav');
```

Заменить JSX:

```tsx
<nav className="nav" role="tablist" aria-label={t('sections')} ref={navRef}>
  {NAV.map((item) => (
    <Button
      key={item.id}
      variant="none"
      role="tab"
      aria-selected={activeTab === item.id}
      onClick={() => onTabChange(item.id)}
    >
      {t(item.labelKey)}
    </Button>
  ))}
</nav>

<div className="top-r" ref={menuRef}>
  <LocaleSwitch />
  <Button
    variant="none"
    className="acct"
    aria-haspopup="menu"
    aria-expanded={menuOpen}
    onClick={() => setMenuOpen((v) => !v)}
  >
    {t('profile')}
  </Button>
  {menuOpen && (
    <div className="acct-menu" role="menu">
      <KeyValue label={t('who')}>{user?.name || t('noName')}</KeyValue>
      <KeyValue label={t('email')}>{user?.email}</KeyValue>
      <Button
        variant="risk"
        style={{ marginTop: 'var(--s3)', width: '100%' }}
        onClick={() => {
          setMenuOpen(false);
          void logout();
        }}
      >
        {t('logout')}
      </Button>
    </div>
  )}
</div>
```

(Единственное реальное изменение внутри блоков — новые `{t(...)}` на месте прежних русских
строк и строка `<LocaleSwitch />` перед кнопкой «Профиль»; остальная разметка, включая
`aria-selected`, `onClick`, идентична текущей.)

- [ ] **Step 3: Typecheck и lint**

Run: `cd frontend && npx tsc --noEmit -p tsconfig.json && npx eslint src/shared/ui/LocaleSwitch.tsx src/widgets/top-nav/TopNav.tsx --max-warnings=0`
Expected: без ошибок.

- [ ] **Step 4: Ручная проверка**

Run: `cd frontend && npm run dev` (или использовать уже запущенный dev-сервер), открыть
приложение, убедиться: в шапке рядом с «Профиль» появился переключатель `RU`/`EN`; клик по
`EN` переводит вкладки навигации и «Профиль»/«Выйти», не ломая активную вкладку; после
перезагрузки страницы выбранный язык сохраняется (стоит в `localStorage['virex-locale']`).

- [ ] **Step 5: Коммит**

```bash
git add frontend/src/shared/ui/LocaleSwitch.tsx frontend/src/widgets/top-nav/TopNav.tsx
git commit -m "i18n: переключатель языка и локализация TopNav"
```

---

## Task 7: Локализация страницы входа

**Files:**
- Modify: `frontend/src/app/login/page.tsx`
- Modify: `frontend/src/features/auth/model/AuthContext.tsx`

**Interfaces:**
- Consumes: неймспейс `auth` (Task 2) через `useTranslations('auth')`; `resolveApiError`
  (Task 3).
- Produces: ничего нового наружу — конечные экраны.

- [ ] **Step 1: Переключить `AuthContext.readError` на `resolveApiError`**

В `frontend/src/features/auth/model/AuthContext.tsx` заменить импорт и функцию:

```ts
import { resolveApiError } from '@/shared/api/http';
```

```ts
async function readError(res: Response): Promise<string> {
  const data = await res.json().catch(() => ({}));
  return resolveApiError(data as { code?: string; message?: string | string[] });
}
```

(Без явного `fallback` — `resolveApiError` сама падает на `errors.generic` из текущего
каталога, если ни `code`, ни `message` не пришли; это и есть замена прежнего жёсткого
`'Ошибка авторизации'`.)

- [ ] **Step 2: Локализовать `login/page.tsx`**

Добавить импорт `import { useTranslations } from 'next-intl';`, внутри компонента —
`const t = useTranslations('auth');`, и заменить точечные места:

```tsx
    } catch (err) {
      setError(err instanceof Error ? err.message : t('genericError'));
    } finally {
```

```tsx
        <div className="mark" style={{ marginBottom: 'var(--s5)' }}>
          {t('brand')}
        </div>

        <h1>{mode === 'login' ? t('loginTitle') : t('registerTitle')}</h1>
        <p className="lede" style={{ marginBottom: 'var(--s4)' }}>
          {mode === 'login' ? t('loginLede') : t('registerLede')}
        </p>

        <form onSubmit={onSubmit}>
          {mode === 'register' && (
            <Field label={t('nameLabel')} htmlFor="name">
```

```tsx
          <Field label={t('emailLabel')} htmlFor="email">
```

```tsx
          <Field label={t('passwordLabel')} htmlFor="password">
```

```tsx
          <Button type="submit" variant="solid" style={{ width: '100%' }} disabled={submitting}>
            {submitting ? t('submitting') : mode === 'login' ? t('submitLogin') : t('submitRegister')}
          </Button>
        </form>

        <Button
          variant="bare"
          style={{ marginTop: 'var(--s3)', width: '100%' }}
          onClick={() => {
            setMode(mode === 'login' ? 'register' : 'login');
            setError(null);
          }}
        >
          {mode === 'login' ? t('switchToRegister') : t('switchToLogin')}
        </Button>
```

Остальная разметка (обёртки `div`/`style`, `Input`-пропы вроде `autoComplete`/`minLength`) не
меняется.

- [ ] **Step 3: Typecheck и lint**

Run: `cd frontend && npx tsc --noEmit -p tsconfig.json && npx eslint src/app/login/page.tsx src/features/auth/model/AuthContext.tsx --max-warnings=0`
Expected: без ошибок.

- [ ] **Step 4: Ручная проверка**

Открыть `/login`, переключить язык на EN переключателем в шапке (он виден и на защищённых
страницах — если сессии ещё нет, можно временно поставить `EN` через
`localStorage.setItem('virex-locale', 'en')` в консоли браузера перед загрузкой `/login`).
Убедиться: заголовок, подписи полей, кнопки и текст-переключатель режима — на английском;
ошибка неверного пароля («Invalid email or password») тоже на английском (проверяет заодно
Task 9 — код на бэкенде уже должен быть смигрирован к этому шагу).

- [ ] **Step 5: Коммит**

```bash
git add frontend/src/app/login/page.tsx frontend/src/features/auth/model/AuthContext.tsx
git commit -m "i18n: локализовать страницу входа"
```

---

## Task 8: Локализация `AuthGuard`

**Files:**
- Modify: `frontend/src/features/auth/ui/AuthGuard.tsx`

**Interfaces:**
- Consumes: неймспейс `common` (Task 2) через `useTranslations('common')`.

- [ ] **Step 1: Локализовать текст ожидания**

```tsx
// frontend/src/features/auth/ui/AuthGuard.tsx
'use client';

import { useEffect, type ReactNode } from 'react';
import { useRouter } from 'next/navigation';
import { useTranslations } from 'next-intl';
import { useAuth } from '../model/AuthContext';

// Wrap protected content. Redirects to /login when there is no session.
export function AuthGuard({ children }: { children: ReactNode }) {
  const { user, loading } = useAuth();
  const router = useRouter();
  const t = useTranslations('common');

  useEffect(() => {
    if (!loading && !user) {
      router.replace('/login');
    }
  }, [loading, user, router]);

  if (loading) {
    // Крутящегося колечка тут быть не может: радиус в этой системе нулевой,
    // так что «спиннер» — это квадрат. Ожидание показывается словом.
    return (
      <div
        style={{
          minHeight: '100vh',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        <span className="lbl">{t('restoringSession')}</span>
      </div>
    );
  }

  if (!user) return null;

  return <>{children}</>;
}
```

- [ ] **Step 2: Typecheck и lint**

Run: `cd frontend && npx tsc --noEmit -p tsconfig.json && npx eslint src/features/auth/ui/AuthGuard.tsx --max-warnings=0`
Expected: без ошибок.

- [ ] **Step 3: Коммит**

```bash
git add frontend/src/features/auth/ui/AuthGuard.tsx
git commit -m "i18n: локализовать AuthGuard"
```

---

## Task 9: Коды ошибок в `auth.service.ts`

**Files:**
- Modify: `backend/src/auth/auth.service.ts`
- Test: `backend/src/auth/auth.service.spec.ts` (создать — файла раньше не было)

**Interfaces:**
- Produces: те же 6 `throw`, но с телом `{ message: string, code: string }` вместо голой
  строки. Коды — контракт с `errors.*` в `frontend/src/shared/i18n/messages/*.json` (Task 2):
  `USER_EXISTS`, `INVALID_CREDENTIALS` (используется в двух местах `login`),
  `REFRESH_TOKEN_MISSING`, `REFRESH_TOKEN_INVALID` (используется в двух местах — «не найден» и
  «гонка при ротации»), `REFRESH_TOKEN_EXPIRED`, `USER_NOT_FOUND`.

- [ ] **Step 1: Написать падающий тест**

```ts
// backend/src/auth/auth.service.spec.ts
import { UnauthorizedException } from '@nestjs/common';
import { AuthService } from './auth.service';

// Минимальные ручные стабы вместо полного PrismaService/JwtService — login с
// несуществующим email не доходит ни до bcrypt, ни до jwt.sign, так что оба
// достаточно смоделировать пустышками нужной формы (`as any`, единственное
// оправданное место для него в этом файле).
describe('AuthService.login', () => {
  it('кидает UnauthorizedException с code INVALID_CREDENTIALS, если пользователя нет', async () => {
    const prisma = { user: { findUnique: async () => null } } as any;
    const service = new AuthService(prisma, {} as any);

    let caught: unknown;
    try {
      await service.login({ email: 'ghost@example.com', password: 'whatever' } as any);
    } catch (err) {
      caught = err;
    }

    expect(caught).toBeInstanceOf(UnauthorizedException);
    expect((caught as UnauthorizedException).getResponse()).toMatchObject({
      code: 'INVALID_CREDENTIALS',
    });
  });
});
```

- [ ] **Step 2: Запустить тест, убедиться, что падает**

Run: `cd backend && npx jest src/auth/auth.service.spec.ts`
Expected: FAIL — `getResponse()` сейчас возвращает строку `'Неверный email или пароль'`, а не
объект с `code`, `toMatchObject` падает.

- [ ] **Step 3: Добавить коды во все 6 throw**

В `backend/src/auth/auth.service.ts`:

```ts
    if (existing) {
      throw new ConflictException({
        message: 'Пользователь с таким email уже существует',
        code: 'USER_EXISTS',
      });
    }
```

```ts
    if (!user) {
      throw new UnauthorizedException({
        message: 'Неверный email или пароль',
        code: 'INVALID_CREDENTIALS',
      });
    }

    const valid = await bcrypt.compare(dto.password, user.password);
    if (!valid) {
      throw new UnauthorizedException({
        message: 'Неверный email или пароль',
        code: 'INVALID_CREDENTIALS',
      });
    }
```

```ts
    if (!rawToken) {
      throw new UnauthorizedException({
        message: 'Refresh-токен отсутствует',
        code: 'REFRESH_TOKEN_MISSING',
      });
    }
```

```ts
    if (!stored) {
      throw new UnauthorizedException({
        message: 'Недействительный refresh-токен',
        code: 'REFRESH_TOKEN_INVALID',
      });
    }

    if (stored.expiresAt.getTime() < Date.now()) {
      await this.prisma.refreshToken
        .delete({ where: { id: stored.id } })
        .catch(() => undefined);
      throw new UnauthorizedException({
        message: 'Refresh-токен истёк',
        code: 'REFRESH_TOKEN_EXPIRED',
      });
    }
```

```ts
    if (!user) {
      throw new UnauthorizedException({
        message: 'Пользователь не найден',
        code: 'USER_NOT_FOUND',
      });
    }
```

И в `issueTokens` (гонка при ротации):

```ts
      } catch {
        throw new UnauthorizedException({
          message: 'Недействительный refresh-токен',
          code: 'REFRESH_TOKEN_INVALID',
        });
      }
```

Обёртки `ConflictException`/`UnauthorizedException` уже импортированы — новых импортов не
требуется. Передача объекта вместо строки — штатный способ задать тело NestJS-исключения
(`HttpException`, см. `getResponse()`); единственное побочное отличие — в JSON-теле ответа
пропадает автоматически подставлявшееся поле `statusCode` (Nest добавляет его только когда
`response` — строка). Ни один фронтенд-код на `body.statusCode` не полагается (проверено
грепом по `frontend/src` — совпадений нет), HTTP-статус самого ответа (`res.status`) при этом
не меняется — он берётся из `exception.getStatus()`, а не из тела.

- [ ] **Step 4: Запустить тест, убедиться, что проходит**

Run: `cd backend && npx jest src/auth/auth.service.spec.ts`
Expected: PASS.

- [ ] **Step 5: Прогнать остальные тесты auth и типы бэкенда**

Run: `cd backend && npx jest src/auth && npx tsc --noEmit -p tsconfig.json`
Expected: PASS / без ошибок типов (если в модуле есть другие `*.spec.ts`, тронутые файлом
косвенно, например `refresh-token-cleanup.service.ts`, — они не читают тело исключения и не
должны сломаться).

- [ ] **Step 6: Коммит**

```bash
git add backend/src/auth/auth.service.ts backend/src/auth/auth.service.spec.ts
git commit -m "i18n: добавить code в исключения AuthService"
```

---

## Task 10: Итоговая проверка Phase 0

**Files:** нет новых — сквозная верификация.

- [ ] **Step 1: Полный прогон тестов фронтенда**

Run: `cd frontend && npm run test`
Expected: все тесты зелёные, включая новые (`locale-storage`, `messages`, `http`,
`format` с новым кейсом).

- [ ] **Step 2: Полный прогон тестов бэкенда**

Run: `cd backend && npx jest`
Expected: все тесты зелёные, включая новый `auth.service.spec.ts`.

- [ ] **Step 3: Полная сборка фронтенда**

Run: `cd frontend && npx next build`
Expected: сборка проходит без ошибок (новая зависимость `next-intl`, несколько
изменённых файлов — по правилу CLAUDE.md это «крупное изменение», билд обязателен перед
завершением фазы).

- [ ] **Step 4: Линт всего затронутого**

Run: `cd frontend && npx eslint src --max-warnings=0`
Expected: без ошибок.

- [ ] **Step 5: Ручной сквозной прогон**

Открыть приложение (`npm run dev` в `frontend`, бэкенд поднят), с чистого `localStorage`:
убедиться, что дефолт — русский; переключить на EN шапкой; разлогиниться, зайти на `/login` —
форма на английском; ввести неверный пароль — сообщение об ошибке на английском; перезагрузить
страницу — язык `EN` сохранился.

- [ ] **Step 6: Коммит (если что-то поправлено по итогам проверки)**

```bash
git add -A
git commit -m "i18n: Phase 0 — правки по итогам сквозной проверки"
```
